apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "docker-users.fullname" . }}-test-connection"
  labels:
    {{- include "docker-users.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "docker-users.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
